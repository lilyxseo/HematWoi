registry=https://registry.npmjs.org/
# Fallback mirror if the above registry is unreachable
# registry=https://registry.npmmirror.com/

# Configure a proxy when behind a corporate firewall
# proxy=http://USER:PASS@proxy.host:port
# https-proxy=http://USER:PASS@proxy.host:port

# Disable strict SSL only if your network intercepts certificates (use with caution)
# strict-ssl=false
